// cspell:disable
import { ModelImage } from "../../core-model";

export const AiModelGemini31FlashImagePreview = ModelImage({
  capabilities: { input: [`text`, `image`], output: [`text`, `image`] },
  name: `gemini-3.1-flash-image-preview`,
});
