# 💬 Notes from a design review

## 🎬 Opening

Capture the argument, not the transcript. Quotes below keep the **decision**, the _tension_, and a `constraint` in
readable form.

## 🧵 Outer thread

> Ship the smaller surface first. **Clarity** beats completeness on day one.
>
> Measure activation before adding settings.
>
> > Nested concern: empty states need a real empty path.
> >
> > Nested detail with `empty-state.md` and _why users bounce_.

## 🌉 Bridge

A short bridge paragraph separates quote clusters so the stream can close one block before the next opens.

## 📌 Quote with a list

> Keep the review concrete:
>
> - Name the user job
> - Name the `success` signal
> - Drop ~~nice-to-have~~ polish until both are clear

## ✅ Close

End outside the quote so trailing prose stays covered: **decide**, then _document_.
