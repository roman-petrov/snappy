# Snappy Bot

Telegram-бот для улучшения контента, созданный специально для контент-мейкеров и владельцев Telegram-каналов.

## Возможности

- 📝 **Изменение стилистики** - деловой, дружеский, юмористический, продающий стили
- ✅ **Исправление ошибок** - орфография, пунктуация, грамматика
- 😊 **Добавление эмодзи** - умное добавление эмодзи в текст
- 📉 **Сокращение текста** - краткая и ёмкая версия
- 📈 **Расширение текста** - более детальная версия
- 🎯 **Улучшение читаемости** - структурирование и форматирование

## Технологии

- TypeScript
- GramIO (Telegram Bot Framework)
- GigaChat AI
- YooKassa (монетизация)

## Архитектура

Проект использует **stateless** архитектуру:

- Данные хранятся только в оперативной памяти
- Нет базы данных
- Простое масштабирование
- Минимальные требования к инфраструктуре

## Структура

```text
snappy/
├── src/
│   ├── handlers/          # Обработчики команд и сообщений
│   │   ├── commands.ts    # Команды бота (/start, /help и т.д.)
│   │   ├── messages.ts    # Обработка текстовых сообщений
│   │   └── callbacks.ts   # Обработка inline-кнопок
│   ├── keyboards/         # Inline-клавиатуры
│   ├── locales/           # Мультиязычность (ru, en)
│   ├── prompts/           # Системные промпты для GigaChat
│   ├── services/          # Сервисы (GigaChat, платежи)
│   │   ├── gigachat.ts    # Интеграция с GigaChat API
│   │   └── payment.ts     # Интеграция с YooKassa
│   ├── storage/           # In-memory хранилище сессий
│   └── index.ts           # Точка входа
└── package.json
```

## Запуск

### Разработка

1. **Создайте тестового бота через [@BotFather](https://t.me/botfather)**
   - Отправьте команду `/newbot`
   - Следуйте инструкциям
   - Скопируйте полученный токен

2. **Настройте конфигурацию:**

Конфигурация загружается из файла `~/.snappy/config.json` (где `~` - домашняя директория пользователя).

```bash
# Создайте директорию для конфигурации (если её нет)
mkdir -p ~/.snappy

# Скопируйте пример конфигурации
cp packages/snappy/config.json.example ~/.snappy/config.json

# Отредактируйте config.json и заполните все необходимые параметры
```

**Важно:** Один и тот же файл конфигурации используется как для разработки, так и для production.

3. **Запустите бота в режиме разработки:**

Из корня монорепозитория:

```bash
bun run dev
```

Или из директории пакета:

```bash
cd packages/snappy
bun run dev
```

### Production

1. **Убедитесь, что конфигурация настроена:**

Конфигурация загружается из того же файла `~/.snappy/config.json`. Убедитесь, что в нём указан токен продакшн-бота и все
необходимые параметры.

2. **Запустите:**

```bash
# Из корня
bun run start

# Или из директории пакета
cd packages/snappy
bun run start
```

## Функции улучшения контента

1. **Стилистика:**
   - Деловой стиль
   - Дружеский стиль
   - Юмористический стиль
   - Продающий стиль
   - Нейтральный стиль

2. **Редактирование:**
   - Исправление ошибок
   - Добавление эмодзи
   - Сокращение текста
   - Расширение текста
   - Улучшение читаемости

## Монетизация

Бот поддерживает монетизацию через YooKassa:

- Бесплатные запросы (настраивается лимит)
- Премиум подписка для безлимитного использования
- Автоматический сброс счетчика каждые 24 часа

## Конфигурация

Конфигурация загружается из файла `~/.snappy/config.json` (где `~` - домашняя директория пользователя).

См. `config.json.example` для примера конфигурации и списка всех параметров.

**Параметры конфигурации:**

- `BOT_TOKEN` - токен Telegram бота (обязательно)
- `GIGACHAT_CLIENT_ID` - идентификатор клиента GigaChat (обязательно)
- `GIGACHAT_CLIENT_SECRET` - секретный ключ GigaChat (обязательно)
- `GIGACHAT_SCOPE` - область доступа GigaChat (по умолчанию: `GIGACHAT_API_PERS`)
- `YOOKASSA_SHOP_ID` - идентификатор магазина YooKassa (опционально)
- `YOOKASSA_SECRET_KEY` - секретный ключ YooKassa (опционально)
- `DEFAULT_LANGUAGE` - язык по умолчанию (`ru` или `en`, по умолчанию: `ru`)
- `FREE_REQUESTS_LIMIT` - лимит бесплатных запросов в день (по умолчанию: `10`)
- `PREMIUM_PRICE` - цена премиум подписки в рублях (по умолчанию: `299`)

## Лицензия

MIT
