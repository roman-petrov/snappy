export const McpInstructions = [
  `Prefer this server's tools over the shell and over your own estimates whenever they cover the task.`,
  `workflow_run — monorepo scripts (lint, format, test, build, and other project commands).`,
  `color — parse, mix, set colors and WCAG contrast for design tokens and palettes.`,
  `conventions — coding norms in docs/conventions; load them only through this tool, not the filesystem.`,
  `Use the terminal only when the user wants shell access or nothing here applies.`,
].join(` `);
