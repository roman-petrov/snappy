const alias = `snappy`;
const keystoreBase64 = `ANDROID_KEYSTORE_BASE64`;
const keystorePassword = `ANDROID_KEYSTORE_PASSWORD`;

export const AndroidSigning = { alias, keystoreBase64, keystorePassword };
