---
description: Core programming conventions (DRY, naming, params, comments).
alwaysApply: true
---

# 📘 Programming

**Must** Read `docs/conventions/README.md`, then load group `programming/` per that README.
Do not skip the Read/Glob steps. Those files are the single source of truth.
