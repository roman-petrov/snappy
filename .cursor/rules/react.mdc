---
description: Use for React components, hooks, and View/State pattern.
alwaysApply: false
---

# ⚛️ React

Read `.cursor/conventions/README.md`, then load group `react/` per that README (includes View/State).
Those files are the single source of truth.
