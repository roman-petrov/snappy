---
name: typescript-programmer
description: TypeScript coding style — functional, minimal, no OOP. Use when writing or reviewing TypeScript: general principles (DRY, type inference, naming, undefined, ===, braces), pure functions, type (not interface), arrow functions, module export conventions.
---

# TypeScript Programmer

## 🎯 Философия

- **Бритва Оккама** — не писать код «на будущее», только то, что нужно сейчас.
- **Простота** — простой и компактный код.
- **Без ООП** — не использовать классы и объектно-ориентированный стиль.

---

## 📐 Общие принципы программирования

### Не дублировать код

- Выносить повторяющуюся логику в функции или общие константы.
- **Один источник правды** для типов и значений: выводить типы из данных, а не дублировать перечисления вручную.

### Вывод типов из значений

Типы по возможности **выводить из значений**, чтобы данные и типы не расходились. Явно аннотировать только когда вывод
невозможен или ухудшает читаемость.

| Ситуация                          | Как выводить                                               |
| --------------------------------- | ---------------------------------------------------------- |
| Литеральный массив/кортеж         | `const X = [...] as const` → `type T = (typeof X)[number]` |
| Объект-константа (ключи/значения) | `keyof typeof OBJ`, `(typeof OBJ)[key]`                    |
| Возвращаемый тип функции          | `ReturnType<typeof fn>`                                    |
| Параметры функции                 | `Parameters<typeof fn>`                                    |

Один пример для массива и объекта:

```ts
const STATUSES = ["pending", "done", "failed"] as const;
type Status = (typeof STATUSES)[number];

const CONFIG = { timeout: 5000, retries: 3 } as const;
type ConfigKey = keyof typeof CONFIG;
```

### Именование файлов

- Файлы TypeScript — **PascalCase**, исключения: `main.ts`, `index.ts`.
- Примеры: `UserService.ts`, `ApiClient.ts`, `FormatDate.ts`, `main.ts`, `index.ts`.

### undefined вместо null

- Не использовать `null`; для «отсутствующего» значения — **`undefined`**.
- Типы `T | null` заменять на `T | undefined` (или опциональное поле без явного типа).

### Строгое сравнение

- Всегда **`===`** и **`!==`**, не `==` / `!=`.

### Условия со скобками

- В **`if`** (и в `else`, `else if`) всегда использовать **фигурные скобки**, даже для одного оператора.

---

## ✍️ Стиль кода

### Функциональный стиль

- Основная единица — **функция**.
- Предпочитать **чистые функции**: выносить в отдельные файлы и покрывать тестами.
- Везде **arrow-функции**, не обычные `function`.

### Типы

- Только **`type`**, не `interface`.
- **Не указывать типы** там, где компилятор выводит их сам.

### Комментарии

- Не комментировать очевидное; только **неочевидные** моменты.
- Язык комментариев — **английский**.

---

## 📦 Правила экспорта модулей

### Без переменных в модуле

- В модуле не использовать **`let`** (только `const` или функции).

### Модуль из чистых функций

Экспорт как **объект с функциями**: `export const <ModuleName> = { fn1, fn2, ... }`. Функции объявлены в том же файле.

### Модуль с побочными эффектами

Экспорт как **замыкание (фабрика)**, возвращающая API: `export const <ModuleName> = (deps?) => ({ method1, method2 })`.
Внутри — `let` в замыкании, сброс через `undefined`, проверки через `===` и скобки в `if`.

```ts
export const Timer = (delay: number) => {
  let id: ReturnType<typeof setInterval> | undefined = undefined;
  const stop = () => {
    if (id !== undefined) {
      clearInterval(id);
      id = undefined;
    }
  };
  return { start, stop };
};
```

---

## ✅ Краткий чеклист

**Принципы**

- [ ] Нет дублирования кода
- [ ] Типы выводятся из значений там, где возможно
- [ ] Файлы в PascalCase (кроме main.ts, index.ts)
- [ ] Используется undefined, не null
- [ ] Сравнения только === / !==
- [ ] У всех if есть фигурные скобки

**Стиль**

- [ ] Нет лишнего кода «на будущее», код простой и короткий
- [ ] Нет классов и ООП
- [ ] Везде arrow-функции
- [ ] Только `type`, без `interface`
- [ ] Типы не указаны там, где выводятся
- [ ] Комментарии только для неочевидного, на английском
- [ ] Чистые функции вынесены и под тестами

**Модули**

- [ ] В модуле нет `let` (кроме замыкания в фабрике)
- [ ] Чистый модуль → `export const X = { fn1, fn2 }`
- [ ] Модуль с сайд-эффектами → `export const X = (deps) => ({ ... })`
