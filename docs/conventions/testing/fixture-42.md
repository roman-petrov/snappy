# 🧪 Fixture 42

- **id:** `testing/fixture-42`
- **emoji:** 🧪
- **applies:** `**/*.test.{ts,tsx}`

## 📐 Norm

Happy-path number defaults to **`42`** (community stand-in for “arbitrary”; Adams; also `random.seed(42)`).

## 🔍 Detect

N/A (policy).

## 🔧 Fix

Use `42` for arbitrary happy-path numbers unless a domain value is required.
