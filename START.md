# Инструкция по запуску Snappy Bot

Данная инструкция описывает все шаги, необходимые для запуска Telegram-бота Snappy.

## Предварительные требования

### 1. Установка Node.js и Bun

Для работы проекта нужны Node.js (для запуска через tsx) и Bun (как пакетный менеджер).

**Установка Node.js:**

- Скачайте и установите с [https://nodejs.org/](https://nodejs.org/) (рекомендуется LTS версия)

**Установка Bun (пакетный менеджер):**

**Windows:**

```powershell
powershell -c "irm bun.sh/install.ps1 | iex"
```

**macOS/Linux:**

```bash
curl -fsSL https://bun.sh/install | bash
```

Проверьте установку:

```bash
node --version
bun --version
```

## Настройка проекта

### 2. Установка зависимостей

Выполните в корневой директории проекта:

```bash
bun install
```

Эта команда установит все необходимые зависимости из `package.json`.

### 3. Создание Telegram ботов

**Важно:** Создайте **два отдельных бота** - для разработки и для production. Это стандартная практика!

#### Бот для разработки:

1. Откройте Telegram и найдите бота [@BotFather](https://t.me/BotFather)
2. Отправьте команду `/newbot`
3. Введите имя: например, `Snappy Dev Bot`
4. Введите username: например, `snappy_dev_bot`
5. Получите токен бота (строка вида `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`)
6. Сохраните этот токен для `.env.dev`

#### Бот для production (опционально на начальном этапе):

1. Повторите те же шаги
2. Введите имя: например, `Snappy Bot`
3. Введите username: например, `snappy_content_bot`
4. Сохраните токен для `.env`

**Зачем два бота?**
- Тестирование изменений без влияния на пользователей
- Hot-reload работает только с dev-ботом
- Невозможно запустить два инстанса с одним токеном

### 4. Получение доступа к GigaChat API

1. Перейдите на сайт [GigaChat](https://developers.sber.ru/portal/products/gigachat)
2. Зарегистрируйтесь или войдите в личный кабинет
3. Создайте новое приложение
4. Получите:
   - `Client ID` (идентификатор клиента)
   - `Client Secret` (секретный ключ)
5. Сохраните эти данные

### 5. Настройка платежной системы YooKassa (опционально)

Для монетизации через YooKassa:

1. Зарегистрируйтесь на [YooKassa](https://yookassa.ru/)
2. Создайте магазин и пройдите модерацию
3. В личном кабинете получите:
   - `Shop ID` (идентификатор магазина)
   - `Secret Key` (секретный ключ)
4. Настройте webhook для уведомлений о платежах (URL вашего сервера + `/webhook`)

**Примечание:** Для тестирования можно не настраивать YooKassa - функция покупки премиума просто не будет работать.

### 6. Конфигурация переменных окружения

Проект поддерживает два окружения:

#### Для разработки (`.env.dev`):

```bash
cd packages/snappy
cp .env.dev.example .env.dev
```

Откройте файл `.env.dev` и заполните токен **dev-бота**:

```env
# Telegram Bot Token для разработки
BOT_TOKEN=токен_dev_бота_от_botfather

# GigaChat API Configuration
GIGACHAT_CLIENT_ID=ваш_client_id
GIGACHAT_CLIENT_SECRET=ваш_client_secret
GIGACHAT_SCOPE=GIGACHAT_API_PERS

# Остальные параметры...
```

#### Для production (`.env`):

```bash
cd packages/snappy
cp .env.example .env
```

Откройте файл `.env` и заполните токен **продакшн-бота**:

```env
# Telegram Bot Token (обязательно)
BOT_TOKEN=ваш_токен_от_botfather

# GigaChat API Configuration (обязательно)
GIGACHAT_CLIENT_ID=ваш_client_id
GIGACHAT_CLIENT_SECRET=ваш_client_secret
GIGACHAT_SCOPE=GIGACHAT_API_PERS

# Payment Configuration - YooKassa (опционально)
YOOKASSA_SHOP_ID=ваш_shop_id
YOOKASSA_SECRET_KEY=ваш_secret_key

# Bot Configuration
DEFAULT_LANGUAGE=ru
FREE_REQUESTS_LIMIT=10
PREMIUM_PRICE=299
```

**Важно:**

- `BOT_TOKEN` - обязательный параметр
- `GIGACHAT_CLIENT_ID` и `GIGACHAT_CLIENT_SECRET` - обязательные параметры
- Параметры YooKassa можно оставить пустыми для тестирования
- `FREE_REQUESTS_LIMIT` - количество бесплатных запросов на пользователя в день (по умолчанию 10)
- `PREMIUM_PRICE` - цена премиум подписки в рублях (по умолчанию 299)

## Запуск бота

### 7. Режим разработки

Для запуска в режиме разработки с автоматической перезагрузкой при изменении кода:

```bash
# Из корня монорепозитория
bun run dev
```

Эта команда:
- Использует `.env.dev` с токеном dev-бота
- Запускает `tsx watch` для автоматической перезагрузки при изменениях
- Любые изменения в коде сразу применяются

**Теперь можете тестировать бота в Telegram!** Найдите своего dev-бота и отправьте `/start`

### 8. Производственный запуск

Для запуска в production режиме:

```bash
# Из корня монорепозитория
bun run start
```

Эта команда:
- Использует `.env` с токеном продакшн-бота
- Запускает `tsx` для выполнения TypeScript напрямую
- Не перезагружается при изменениях

## Проверка качества кода

### 10. Запуск всех линтеров

Перед деплоем рекомендуется проверить код:

```bash
bun run ci
```

Эта команда последовательно запустит:

- TypeScript Compiler (проверка типов)
- ESLint (проверка TypeScript кода)
- CSpell (проверка орфографии)
- JSCPD (поиск дублирования кода)
- Markdownlint (проверка markdown файлов)

Индивидуальные команды для запуска линтеров:

```bash
bun run lint:tsc         # Только TypeScript Compiler
bun run lint:eslint      # Только ESLint
bun run lint:prettier    # Только Prettier
bun run lint:cspell      # Только CSpell
bun run lint:jscpd       # Только JSCPD
bun run lint:markdown    # Только Markdownlint
```

Команды для форматирования:

```bash
bun run format           # Автоматическое форматирование всех файлов
bun run format:check     # Проверка форматирования без изменений
```

## Использование бота

### 11. Команды бота

После запуска бот поддерживает следующие команды:

- `/start` - Начать работу с ботом, показать приветствие
- `/help` - Показать справку по командам
- `/balance` - Проверить количество оставшихся бесплатных запросов
- `/premium` - Купить премиум подписку (если настроен YooKassa)
- `/language` - Сменить язык интерфейса (русский/английский)

### 12. Работа с текстом

1. Отправьте боту любой текст
2. Выберите нужную функцию из появившейся клавиатуры:
   - **Деловой стиль** - переписать в официально-деловом стиле
   - **Дружеский стиль** - сделать текст более теплым и приветливым
   - **Юмористический стиль** - добавить юмор и легкость
   - **Продающий стиль** - превратить в продающий текст
   - **Нейтральный стиль** - сделать объективным и информативным
   - **Исправить ошибки** - исправить орфографию, пунктуацию и грамматику
   - **Добавить эмодзи** - добавить подходящие эмодзи
   - **Сократить текст** - создать краткую версию
   - **Расширить текст** - создать более детальную версию
   - **Улучшить читаемость** - структурировать и упростить текст
3. Получите обработанный результат

## Деплой на сервер

### 13. Использование PM2 (рекомендуется)

Для запуска бота в фоновом режиме на сервере:

```bash
# Установка PM2
npm install -g pm2

# Запуск бота
pm2 start "bun run start" --name snappy-bot

# Автозапуск при перезагрузке сервера
pm2 startup
pm2 save

# Просмотр логов
pm2 logs snappy-bot

# Перезапуск
pm2 restart snappy-bot

# Остановка
pm2 stop snappy-bot
```

### 14. Использование systemd (альтернатива)

Создайте файл `/etc/systemd/system/snappy-bot.service`:

```ini
[Unit]
Description=Snappy Telegram Bot
After=network.target

[Service]
Type=simple
User=your-user
WorkingDirectory=/path/to/snappy
ExecStart=/usr/local/bin/bun run start
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

Затем:

```bash
sudo systemctl enable snappy-bot
sudo systemctl start snappy-bot
sudo systemctl status snappy-bot
```

## Troubleshooting (Решение проблем)

### Бот не запускается

1. Проверьте, что все переменные в `.env` заполнены корректно
2. Убедитесь, что токен бота действителен
3. Проверьте логи на наличие ошибок

### Ошибки GigaChat API

1. Убедитесь, что `Client ID` и `Client Secret` указаны правильно
2. Проверьте, что у вас есть доступ к GigaChat API
3. Попробуйте обновить токен доступа (бот делает это автоматически)

### Ошибки при установке зависимостей

```bash
# Очистите кэш и переустановите
rm -rf node_modules bun.lockb
bun install
```

### Проблемы с линтерами

```bash
# Установите dev-зависимости
bun install --dev

# Проверьте версию Node.js/Bun
bun --version
```

## Дополнительная информация

### Архитектура проекта

Проект использует **stateless** архитектуру:

- Данные пользователей хранятся только в оперативной памяти
- Нет постоянной базы данных
- При перезапуске бота счетчики запросов сбрасываются
- Сессии автоматически очищаются через 24 часа

### Ограничения бесплатной версии

- 10 запросов в день на пользователя (настраивается в `.env`)
- Счетчик сбрасывается каждые 24 часа
- При перезапуске бота счетчики обнуляются

### Монетизация

Бот поддерживает интеграцию с YooKassa для продажи премиум подписки:

- Премиум дает безлимитные запросы
- Цена настраивается в переменной `PREMIUM_PRICE`
- Требуется настройка webhook для автоматического подтверждения платежей

## Поддержка

При возникновении вопросов или проблем:

1. Проверьте все шаги в данной инструкции
2. Убедитесь, что используете последнюю версию Bun
3. Проверьте логи бота на наличие ошибок

---

**Готово!** Ваш бот Snappy готов к использованию. 🚀
