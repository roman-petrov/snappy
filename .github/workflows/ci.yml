name: ✅ CI

on:
  workflow_dispatch: # Только ручной запуск через GitHub UI

jobs:
  ci:
    name: ${{ matrix.task.name }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        task:
          - name: 🧪 Run Tests
            arg: test
          - name: 📘 TypeScript Check
            arg: lint:tsc
          - name: 📐 ESLint Check
            arg: lint:eslint
          - name: 💅 Prettier Check
            arg: lint:prettier
          - name: 📝 CSpell Check
            arg: lint:cspell
          - name: 🔍 JSCPD Check
            arg: lint:jscpd
          - name: 📄 Markdown Lint Check
            arg: lint:markdown

    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      - name: 🐰 Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: 📦 Install dependencies
        run: bun install --frozen-lockfile

      - name: ▶️ Run ${{ matrix.task.name }}
        run: bun run packages/do/src/main.cli.ts ${{ matrix.task.arg }}
