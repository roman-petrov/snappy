name: Setup Android
description: Fix gradlew permissions and Java 21 for Gradle (Android build, Spotless)
runs:
  using: composite
  steps:
    - name: 🔧 Fix gradlew permissions
      shell: bash
      run: chmod +x packages/app-android/android/gradlew

    - name: ☕ Use Java 21
      shell: bash
      run: echo "JAVA_HOME=$JAVA_HOME_21_X64" >> $GITHUB_ENV
