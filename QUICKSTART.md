# Быстрый старт 🚀

## Для разработки

### 1. Создайте dev-бота
```
1. Откройте @BotFather в Telegram
2. /newbot → имя: "Snappy Dev" → username: "snappy_dev_bot"
3. Скопируйте токен
```

### 2. Настройте проект
```bash
# Установите зависимости
bun install

# Создайте конфиг для разработки
mkdir -p ~/.snappy
cp packages/snappy/config.json.example ~/.snappy/config.json

# Отредактируйте ~/.snappy/config.json:
# - Вставьте токен dev-бота в BOT_TOKEN
# - Добавьте GIGACHAT_CLIENT_ID и GIGACHAT_CLIENT_SECRET
# - Заполните остальные параметры при необходимости
```

### 3. Запустите бота
```bash
# Из корня проекта
bun run dev
```

✅ **Готово!** Бот запущен с hot-reload. Изменения в коде применяются автоматически.

## Для production

### 1. Создайте prod-бота
```
1. @BotFather → /newbot → имя: "Snappy" → username: "snappy_bot"
2. Скопируйте токен
```

### 2. Настройте продакшн
```bash
# Убедитесь, что конфигурация настроена
# Конфигурация загружается из ~/.snappy/config.json
# (используется тот же файл, что и для разработки)

# Отредактируйте ~/.snappy/config.json с токеном продакшн-бота
```

### 3. Запустите
```bash
# Из корня проекта
bun run start
```

## Команды разработки

```bash
bun run dev          # Запуск в dev режиме (hot-reload)
bun run start        # Запуск в production режиме
bun run format       # Форматирование кода
bun run ci           # Проверка всех линтеров
```

## Полная документация

- [START.md](./START.md) - детальная инструкция по настройке
- [README.md](./README.md) - общая информация о проекте
- [packages/snappy/README.md](./packages/snappy/README.md) - документация пакета
